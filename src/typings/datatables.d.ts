// typings/datatables.d.ts
declare module 'datatables.net' {
    interface DataTable {
      // Add any custom methods or properties if needed
    }
  
    interface DataTableSettings {
      // Add any custom settings or properties if needed
    }
  }
  